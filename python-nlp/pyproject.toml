[project]
name = "harivaidya"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.11.0",
    "transformers>=5.5.4",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
